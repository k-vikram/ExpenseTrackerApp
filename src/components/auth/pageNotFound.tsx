import React, { ReactElement } from "react";

const PageNotFound = (): ReactElement | null => {
    return <h4 className="my-6 text-2xl font-extrabold text-gray-700">Page Not Found!</h4>
}

export default PageNotFound;